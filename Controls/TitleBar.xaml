<UserControl x:Class="BatUtils.Controls.TitleBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
             Height="32">
    <UserControl.Resources>
        <Style x:Key="CaptionButton" TargetType="Button">
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TitleBarButtonForegroundBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Cursor" Value="Arrow"/>
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource TitleBarHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource TitleBarPressedBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="CloseCaptionButton"
               TargetType="Button"
               BasedOn="{StaticResource CaptionButton}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background"
                            Value="{DynamicResource CloseHoverBrush}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background"
                            Value="{DynamicResource ClosePressedBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Border x:Name="RootBorder" Background="{DynamicResource TitleBarBackgroundBrush}">
        <DockPanel LastChildFill="True">
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" shell:WindowChrome.IsHitTestVisibleInChrome="True">
                <Button Style="{StaticResource CaptionButton}" Click="MinimizeButton_Click">
                    <Rectangle Width="10" Height="1.5" Fill="{DynamicResource TitleBarButtonForegroundBrush}" VerticalAlignment="Center"/>
                </Button>
                <Button x:Name="MaximizeButton" Style="{StaticResource CaptionButton}" Click="MaximizeButton_Click">
                    <Grid Width="12" Height="12">
                        <Rectangle x:Name="MaximizeIcon" Width="10" Height="10" Stroke="{DynamicResource TitleBarButtonForegroundBrush}" StrokeThickness="1" Fill="Transparent"/>
                        <Grid x:Name="RestoreIcon" Visibility="Collapsed">
                            <Rectangle Width="8" Height="8" Stroke="{DynamicResource TitleBarForegroundBrush}" StrokeThickness="1" HorizontalAlignment="Right" VerticalAlignment="Top"/>
                            <Rectangle Width="8" Height="8" Stroke="{DynamicResource TitleBarForegroundBrush}" StrokeThickness="1" Fill="{DynamicResource WindowBackgroundBrush}" HorizontalAlignment="Left" VerticalAlignment="Bottom"/>
                        </Grid>
                    </Grid>
                </Button>
                <Button Style="{StaticResource CloseCaptionButton}" Click="CloseButton_Click">
                    <Grid Width="10" Height="10">
                        <Line X1="0" Y1="0" X2="10" Y2="10" Stroke="{DynamicResource CloseForegroundBrush}" StrokeThickness="1.5"/>
                        <Line X1="10" Y1="0" X2="0" Y2="10" Stroke="{DynamicResource CloseForegroundBrush}" StrokeThickness="1.5"/>
                    </Grid>
                </Button>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="12,0,0,0" VerticalAlignment="Center">
                <Image Source="/Assets/ant.ico" Width="16" Height="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                <TextBlock Text="BAT Utils" Foreground="{DynamicResource TitleBarForegroundBrush}" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"/>
            </StackPanel>
        </DockPanel>
    </Border>
</UserControl>